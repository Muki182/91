# 男男性服务全档案系统 - 部署指南

## 项目概述
这是一个完整的男男性服务预约管理系统，包含前端界面和后端API服务。系统支持服务人员档案管理、服务项目展示、在线预约、数据统计等功能。

## 系统架构
- **前端**: HTML5 + CSS3 + JavaScript (原生)
- **后端**: Node.js + Express.js
- **数据存储**: JSON文件（自动创建和管理）
- **API接口**: RESTful API

## 文件结构
```
111/
├── index.html          # 主页面
├── server.js           # 后端服务器
├── package.json        # 项目配置
├── styles.css          # 样式文件
├── start.bat          # 启动脚本
├── start-dev.bat      # 开发模式启动
├── install.bat        # 依赖安装脚本
├── test-server.js     # API测试脚本
├── README.md          # 项目说明
└── data.json          # 数据文件（自动创建）
```

## 快速启动

### 方法一：使用批处理文件（推荐）

1. **安装依赖**
   ```
   双击运行 install.bat
   ```

2. **启动服务器**
   ```
   双击运行 start.bat
   ```

3. **访问系统**
   ```
   打开浏览器访问: http://localhost:3000
   ```

### 方法二：手动启动

1. **安装依赖**
   ```bash
   npm install
   ```

2. **启动服务器**
   ```bash
   node server.js
   ```

3. **访问系统**
   ```
   打开浏览器访问: http://localhost:3000
   ```

## 功能特性

### 前端功能
- 📊 **数据统计**: 实时显示服务人员、服务项目、预约数量
- 👥 **人员档案**: 15名专业服务人员详细信息展示
- 💼 **服务项目**: 20种专业服务项目分类展示
- 🔍 **智能搜索**: 支持按姓名、标签、描述搜索
- 📅 **在线预约**: 完整的预约流程和表单验证
- 📱 **响应式设计**: 适配手机、平板、电脑
- 🎨 **现代化UI**: 美观的渐变背景和卡片设计

### 后端API
- `GET /api/stats` - 获取统计数据
- `GET /api/staff` - 获取服务人员列表
- `GET /api/services` - 获取服务项目列表
- `GET /api/bookings` - 获取预约列表
- `POST /api/bookings` - 创建新预约
- `PUT /api/bookings/:id` - 更新预约状态
- `DELETE /api/bookings/:id` - 删除预约
- `GET /api/staff/search?q=关键词` - 搜索服务人员
- `GET /api/services/search?q=关键词` - 搜索服务项目

## 数据管理

### 自动数据初始化
- 首次启动时自动创建 `data.json` 文件
- 包含15名服务人员、20种服务项目的初始数据
- 支持离线模式（API不可用时使用本地存储）

### 数据持久化
- 所有数据自动保存到 `data.json` 文件
- 支持数据备份和恢复
- 预约记录自动编号和时间戳记录

## 测试系统

### API测试
```bash
node test-server.js
```

测试内容：
- 统计信息接口
- 服务人员列表
- 服务项目列表
- 预约记录管理
- 搜索功能
- 创建预约

### 功能测试清单
- [ ] 首页加载和数据统计显示
- [ ] 服务人员卡片展示和搜索
- [ ] 服务项目卡片展示和搜索
- [ ] 预约表单填写和提交
- [ ] 预约记录查看和管理
- [ ] 响应式布局适配

## 系统配置

### 端口配置
默认端口: 3000
如需修改，编辑 `server.js` 中的 `PORT` 变量

### 数据文件路径
默认数据文件: `data.json`
如需修改，编辑 `server.js` 中的 `DATA_FILE` 变量

## 故障排除

### 常见问题

1. **端口被占用**
   ```
   错误: listen EADDRINUSE :::3000
   解决: 修改 server.js 中的端口号
   ```

2. **依赖安装失败**
   ```
   错误: npm install 失败
   解决: 检查网络连接，或使用淘宝镜像
   npm install --registry=https://registry.npmmirror.com
   ```

3. **API连接失败**
   ```
   前端显示"网络连接错误"
   解决: 确保服务器已启动，检查防火墙设置
   ```

### 日志查看
服务器启动时会显示：
- 服务人员数量
- 服务项目数量
- 预约记录数量
- 数据文件路径

## 安全说明

### 数据安全
- 所有敏感数据仅存储在本地
- 支持离线模式运行
- 预约记录包含时间戳和唯一编号

### 隐私保护
- 不收集用户个人信息
- 联系方式仅用于预约确认
- 支持预约记录的删除功能

## 扩展开发

### 添加新服务人员
编辑 `server.js` 中的 `staff` 数组，添加新的服务人员对象

### 添加新服务项目
编辑 `server.js` 中的 `services` 数组，添加新的服务项目对象

### 自定义样式
修改 `styles.css` 文件，调整界面样式和配色方案

## 技术栈详情

### 前端技术
- HTML5: 语义化标签和现代结构
- CSS3: Flexbox布局、Grid布局、渐变、动画
- JavaScript: ES6+ 语法、Fetch API、事件处理
- 图标库: Tabler Icons
- 字体: Google Fonts (Inter + Noto Sans SC)

### 后端技术
- Node.js: 运行时环境
- Express.js: Web框架
- CORS: 跨域支持
- 文件系统: 数据持久化

## 版本信息
- 当前版本: v1.0.0
- 创建时间: 2025年12月
- 最后更新: 2025年12月

---

**注意**: 本项目仅供技术学习和演示使用，请遵守相关法律法规。